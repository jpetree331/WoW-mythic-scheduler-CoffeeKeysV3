name: Verify Coffee and Keys
on: [push, pull_request]
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run verify:vercel
      - run: npm run check
      - run: npm audit --audit-level=high
  postgres:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: coffeekeys_test
          POSTGRES_PASSWORD: isolated_ci_password
          POSTGRES_DB: coffeekeys_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U coffeekeys_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgresql://coffeekeys_test:isolated_ci_password@127.0.0.1:5432/coffeekeys_test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm test
